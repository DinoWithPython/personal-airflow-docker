Для первичной настройки и установки контейнера(в дальнейшем можно запускать без --build)
docker compose up --build

Для остановки контейнера
docker compose down

ссылка на иррфлоу
http://localhost:8080/